<p style="color: green"><%= notice %></p>

<%= image_tag @user.cover , size:'150x150' %>

<%= render @user %>

<div>
 <%= link_to t("user.index.edit"), @user %> |
  <%= link_to t("user.index.back"), users_path %>
<br>
  <%= button_to t('Destroy'), @user, method: :delete %>
  <br>
</div>
