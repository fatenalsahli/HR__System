<p style="color: green"><%= notice %></p>

<%= render @target %>

<div>
  <%= link_to t("activerecord.attributes.target.edittarget"), edit_target_path(@target) %> |
  <%= link_to  t("activerecord.attributes.target.backtarget") , targets_path %>

  <%= button_to t('Destroy'), @target, method: :delete %>
</div>
