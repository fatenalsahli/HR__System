<div class="container">
<h1>Divisions</h1>

</span>
</a>

<table class="table">
  <thead class="thead-dark">
      <tr>
       <th>ID</th>
        <th>Name</th>
        <th>Description</th>
        <th>Teams</th>
        <th>Manager</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>
    <tbody>
     <% @divisions.each do |division| %>
      <tr>
        <td> <%=division.id%></td>
        <td><%=division.name%></td>
        <td><%=division.description%></td>
        <td><%= division.teams.collect(&:name).join(',') %></td>

        <td><%=division.employee_id%></td>
        <td><%= link_to 'new', new_division_path(division)%></td>
        <td><%= link_to 'Edit', edit_division_path(division)%></td>
        <td><%= link_to 'Destroy', division, :confirm => 'Are you sure?', :method => :delete %></td>


      </tr>
    <% end %>
    
    </tbody>
  </table>
   <a href="/" class="mt-5">&#8592; Back to Home</a>
 
  <%= link_to 'new', new_division_path(@divisions)%>
</div>
